# Android / Gradle build artifacts — never commit these.
.gradle/
build/
app/build/
/local.properties
/captures
.externalNativeBuild/
.cxx/

# Android Studio / IntelliJ
.idea/
*.iml

# The wrapper jar is a binary regenerated by Android Studio on first sync
# (or `gradle wrapper`); we ship only gradle-wrapper.properties.
gradle/wrapper/gradle-wrapper.jar
